import { StyleSheet } from 'react-native';

const colors = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#5838A0',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 10
      },
    statusBar:{
        backgroundColor: '#5838A0',
        color: '#fff',
    },
})
export default colors;